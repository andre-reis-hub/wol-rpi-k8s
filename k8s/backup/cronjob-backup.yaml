apiVersion: batch/v1
kind: CronJob
metadata:
  name: wol-backup
  namespace: backup
spec:
  # Diario as 3h da manha (horario do cluster - conferir TZ)
  schedule: "0 3 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          annotations:
            vault.hashicorp.com/agent-inject: "true"
            vault.hashicorp.com/role: "backup"
            vault.hashicorp.com/agent-inject-secret-restic-password: "wol/data/backup"
            vault.hashicorp.com/agent-inject-template-restic-password: |
              {{- with secret "wol/data/backup" -}}
              {{ index .Data.data "restic-password" }}
              {{- end -}}
            vault.hashicorp.com/agent-inject-secret-rclone-conf: "wol/data/rclone"
            vault.hashicorp.com/agent-inject-template-rclone-conf: |
              {{- with secret "wol/data/rclone" -}}
              {{ index .Data.data "rclone-conf" }}
              {{- end -}}
            vault.hashicorp.com/agent-pre-populate-only: "true"
        spec:
          serviceAccountName: backup-sa
          restartPolicy: Never
          containers:
            - name: backup
              image: restic/restic:latest
              command: ["/bin/sh", "-c"]
              args:
                - |
                  apk add --no-cache rclone bash >/dev/null 2>&1 || true
                  bash /scripts/backup.sh
              volumeMounts:
                - name: script
                  mountPath: /scripts
                - name: palworld
                  mountPath: /data/palworld
                  readOnly: true
                - name: abiotic
                  mountPath: /data/abiotic-factor
                  readOnly: true
                - name: valheim
                  mountPath: /data/valheim
                  readOnly: true
          volumes:
            - name: script
              configMap:
                name: backup-script
                defaultMode: 0755
            - name: palworld
              hostPath:
                path: /var/lib/rancher/palworld
                type: Directory
            - name: abiotic
              hostPath:
                path: /var/lib/rancher/abiotic-factor
                type: Directory
            - name: valheim
              hostPath:
                path: /var/lib/rancher/valheim
                type: DirectoryOrCreate
